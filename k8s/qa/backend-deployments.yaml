apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: mfe-demo-qa
spec:
  replicas: 1
  selector:
    matchLabels: { app: product-service }
  template:
    metadata:
      labels: { app: product-service }
    spec:
      containers:
        - name: product-service
          image: ghcr.io/your-org/product-service:qa
          ports: [{ containerPort: 8081 }]
          env:
            - { name: SPRING_R2DBC_URL, value: "r2dbc:postgresql://postgres:5432/appdb?schema=product_schema" }
            - { name: SPRING_R2DBC_USERNAME, value: "product_service" }
            - name: SPRING_R2DBC_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: PRODUCT_SERVICE_DB_PASSWORD } }
            - { name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI, value: "http://keycloak:8080/realms/microfrontend-demo" }
            - { name: SERVER_PORT, value: "8081" }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: mfe-demo-qa
spec:
  selector: { app: product-service }
  ports: [{ port: 8081, targetPort: 8081 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: mfe-demo-qa
spec:
  replicas: 1
  selector:
    matchLabels: { app: order-service }
  template:
    metadata:
      labels: { app: order-service }
    spec:
      containers:
        - name: order-service
          image: ghcr.io/your-org/order-service:qa
          ports: [{ containerPort: 8082 }]
          env:
            - { name: SPRING_R2DBC_URL, value: "r2dbc:postgresql://postgres:5432/appdb?schema=order_schema" }
            - { name: SPRING_R2DBC_USERNAME, value: "order_service" }
            - name: SPRING_R2DBC_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: ORDER_SERVICE_DB_PASSWORD } }
            - { name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI, value: "http://keycloak:8080/realms/microfrontend-demo" }
            - { name: PRODUCT_SERVICE_GRAPHQL_URL, value: "http://product-service:8081/graphql" }
            - { name: SERVER_PORT, value: "8082" }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: mfe-demo-qa
spec:
  selector: { app: order-service }
  ports: [{ port: 8082, targetPort: 8082 }]
---
# Extends product-service's Product entity with an `inventory` field —
# see backend/inventory-service/.../ProductInventoryResolver.java and
# infra/apollo-router/inventory.graphql. Owns no entity itself, has no
# dependency on product-service/order-service being up first (federation
# entity extension is resolved by Apollo Router at query time, not at
# this pod's startup).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: mfe-demo-qa
spec:
  replicas: 1
  selector:
    matchLabels: { app: inventory-service }
  template:
    metadata:
      labels: { app: inventory-service }
    spec:
      containers:
        - name: inventory-service
          image: ghcr.io/your-org/inventory-service:qa
          ports: [{ containerPort: 8083 }]
          env:
            - { name: SPRING_R2DBC_URL, value: "r2dbc:postgresql://postgres:5432/appdb?schema=inventory_schema" }
            - { name: SPRING_R2DBC_USERNAME, value: "inventory_service" }
            - name: SPRING_R2DBC_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: INVENTORY_SERVICE_DB_PASSWORD } }
            - { name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI, value: "http://keycloak:8080/realms/microfrontend-demo" }
            - { name: SERVER_PORT, value: "8083" }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8083 }
            initialDelaySeconds: 15
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: mfe-demo-qa
spec:
  selector: { app: inventory-service }
  ports: [{ port: 8083, targetPort: 8083 }]
